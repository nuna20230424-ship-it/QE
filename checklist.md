# 인증 일정 대시보드 체크리스트

## 1차 구축
- [x] 프로젝트 구조 생성 (Node + Express + better-sqlite3)
- [x] DB 계층 (`db.js`) — requests 테이블 + CRUD
- [x] REST API (`server.js`) — 목록/조회/등록/수정/삭제 + 검증
- [x] 프런트 HTML/CSS/JS — 현황 보드 / 일정표 / 등록·상세 모달
- [x] 역할 토글 (의뢰자 / 테스터) 편집 영역 제한
- [x] 의존성 설치 (`npm install`)
- [x] 로컬 기동 및 API 동작 검증
- [x] 브라우저 등록→예약→진행→완료 흐름 검증
- [x] Mac Mini 실제 IP 확정 → 172.16.3.136 (접속 http://172.16.3.136:3000)
- [x] Mac Mini 배포 및 사내 접속 확인 (172.16.3.136:3001, 2026-06-23)
- [ ] launchd 상시 구동 설정 (재부팅·크래시 자동 복구)

## 2차 보완 (2026-06-23 착수 / 2026-06-24 구현·검증 완료)
- [x] 월간 캘린더 뷰 (경량 자체구현)
- [x] 이메일 알림 (예약확정·완료, nodemailer+Gmail, config 미설정 시 생략)
- [x] 요약 위젯 + 상태 전환 타임스탬프(확정/시작/완료) + 평균 소요일
- [x] 변경 이력 로그(history 테이블) + 상세 모달 표시
- [x] SQLite 일 1회 자동 백업 (backups/)
- [x] 스모크 테스트 12건 통과 + 전체 JS 구문검사 통과
- [ ] Mac Mini 재배포 (npm install 재실행 필요: nodemailer 추가)
- [ ] (선택) config.json 작성해 이메일 알림 활성화

## 3차 메뉴변경 (2026-06-25 구현·검증 완료)
- [x] 의뢰자 드롭다운(기존 데이터 자동채움) + 직접입력
- [x] 담당 테스터 드롭다운(이해찬·이은경·김지윤·조진원·조아라·문유림) + 직접입력
- [x] 상태에 '중단' 추가 (보라색, 활성부하 집계 제외)
- [x] Test type '파생' / Test 목적 'self' 추가
- [x] 완료 일정(completed_date) 필드 + 판정(Pass/Fail) 드롭다운
- [x] 현황보드·일정표에 완료건 Pass(파란)/Fail(빨강) 배지
- [x] 스모크 8건 통과 + 전체 구문검사 통과 (스키마 자동 마이그레이션 포함)

## 4차 현황보고 (2026-07-08 구현)
- [x] 현황 보드 칼럼별 5개까지만 노출 + "+N개 더보기/접기" 토글
- [x] `db.js` reportData(from,to) — 기간 내 완료(Pass/Fail)·현재 진행중 집계
- [x] `report.js` — 일일(오늘)·주간(이번 주 월~일) 기간계산 + HTML 생성(앱·메일 공용)
- [x] 일일보고·주간보고 탭 + 뷰(새로고침·즉시발송 버튼)
- [x] Fail 모델은 진행사항+결과코멘트 상세 표기
- [x] `scheduler.js` — 매일 19시 일일+주간 리포트 자동 메일
- [x] 수신: nuna20230424@gmail.com, keonhee.cho@kaongroup.com (config.reportTo로 재정의 가능)
- [ ] Mac Mini 재배포 (config.json에 smtp 채워야 실제 발송)

## 5차 고도화 (2026-08-26 구현 · 작업지시서 4개 Task)
- [x] Task 1 — Test 목적에 `MR` 옵션 추가
- [x] Task 2 — 모델명 입력 자동 목록화 (`/api/options` + datalist, DISTINCT로 중복 제거)
- [x] Task 3 — 완료 진행/결과를 `진행차수 3차, Pass` 형태로 표기 (보드 카드·일정표·리포트 3곳)
- [x] Task 4-1 — `인증 통계` 탭: 모델×인증별 진행차수·Fail 횟수·Pass율·Fail율 (표 + 비율 바)
- [x] Task 4-2 — 주간보고에 `모델별 인증 현황` 섹션 분리 (해당 주차 월~금 기준)
- [x] 0 나눗셈 방어 (`pct()` 분모 0 → 0 반환) + 빈 DB 케이스 테스트
- [x] 집계 성능 — `idx_requests_model_cert` 인덱스 + SQL GROUP BY 집계 (앱단 Full Scan 제거)
- [x] `npm test` 스모크 31건 통과 + 전체 JS 구문검사 통과
- [x] 인증 통계에 주단위(월~금) 명시 — `주간` / `전체 누적` 토글 + `‹ 8/24~8/28 ›` 주 이동
- [x] 인증 통계 엑셀(CSV) 다운로드 — 화면에 보이는 기간 그대로, 대상 기간·합계 포함
- [x] 프런트 `weekRangeOf(0)` ↔ 서버 `workWeekRange()` 주 경계 일치 대조 통과
- [x] 라이브 서버 E2E 32건 통과 (등록→목록 노출→통계→주간보고→기존 회귀)
- [ ] Mac Mini 재배포 (`npm install` 후 재기동)
- [ ] 브라우저 UI 육안 확인 (모델명 드롭다운·진행차수 배지·통계 주 이동·엑셀 다운로드)

## 향후(요청 시)
- [ ] 사용자 인증/로그인 (현재는 LAN 신뢰 기반, 인증 없음)
- [ ] Jira 티켓 링크 / 거증 첨부 (testrun 연계)
- [ ] 충돌·쿼터(테스트 슬롯 한정 시)
