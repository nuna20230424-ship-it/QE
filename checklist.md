# 인증 일정 대시보드 체크리스트

## 1차 구축
- [x] 프로젝트 구조 생성 (Node + Express + better-sqlite3)
- [x] DB 계층 (`db.js`) — requests 테이블 + CRUD
- [x] REST API (`server.js`) — 목록/조회/등록/수정/삭제 + 검증
- [x] 프런트 HTML/CSS/JS — 현황 보드 / 일정표 / 등록·상세 모달
- [x] 역할 토글 (의뢰자 / 테스터) 편집 영역 제한
- [x] 의존성 설치 (`npm install`)
- [x] 로컬 기동 및 API 동작 검증
- [x] 브라우저 등록→예약→진행→완료 흐름 검증
- [x] Mac Mini 실제 IP 확정 → 172.16.3.136 (접속 http://172.16.3.136:3000)
- [x] Mac Mini 배포 및 사내 접속 확인 (172.16.3.136:3001, 2026-06-23)
- [ ] launchd 상시 구동 설정 (재부팅·크래시 자동 복구)

## 2차 보완 (2026-06-23 착수 / 2026-06-24 구현·검증 완료)
- [x] 월간 캘린더 뷰 (경량 자체구현)
- [x] 이메일 알림 (예약확정·완료, nodemailer+Gmail, config 미설정 시 생략)
- [x] 요약 위젯 + 상태 전환 타임스탬프(확정/시작/완료) + 평균 소요일
- [x] 변경 이력 로그(history 테이블) + 상세 모달 표시
- [x] SQLite 일 1회 자동 백업 (backups/)
- [x] 스모크 테스트 12건 통과 + 전체 JS 구문검사 통과
- [ ] Mac Mini 재배포 (npm install 재실행 필요: nodemailer 추가)
- [ ] (선택) config.json 작성해 이메일 알림 활성화

## 3차 메뉴변경 (2026-06-25 구현·검증 완료)
- [x] 의뢰자 드롭다운(기존 데이터 자동채움) + 직접입력
- [x] 담당 테스터 드롭다운(이해찬·이은경·김지윤·조진원·조아라·문유림) + 직접입력
- [x] 상태에 '중단' 추가 (보라색, 활성부하 집계 제외)
- [x] Test type '파생' / Test 목적 'self' 추가
- [x] 완료 일정(completed_date) 필드 + 판정(Pass/Fail) 드롭다운
- [x] 현황보드·일정표에 완료건 Pass(파란)/Fail(빨강) 배지
- [x] 스모크 8건 통과 + 전체 구문검사 통과 (스키마 자동 마이그레이션 포함)

## 4차 현황보고 (2026-07-08 구현)
- [x] 현황 보드 칼럼별 5개까지만 노출 + "+N개 더보기/접기" 토글
- [x] `db.js` reportData(from,to) — 기간 내 완료(Pass/Fail)·현재 진행중 집계
- [x] `report.js` — 일일(오늘)·주간(이번 주 월~일) 기간계산 + HTML 생성(앱·메일 공용)
- [x] 일일보고·주간보고 탭 + 뷰(새로고침·즉시발송 버튼)
- [x] Fail 모델은 진행사항+결과코멘트 상세 표기
- [x] `scheduler.js` — 매일 19시 일일+주간 리포트 자동 메일
- [x] 수신: nuna20230424@gmail.com, keonhee.cho@kaongroup.com (config.reportTo로 재정의 가능) — **8차에서 개인 Gmail 제거됨**
- [ ] Mac Mini 재배포 (config.json에 smtp 채워야 실제 발송)

## 5차 고도화 (2026-08-26 구현 · 작업지시서 4개 Task)
- [x] Task 1 — Test 목적에 `MR` 옵션 추가
- [x] Task 2 — 모델명 입력 자동 목록화 (`/api/options` + datalist, DISTINCT로 중복 제거)
- [x] Task 3 — 완료 진행/결과를 `진행차수 3차, Pass` 형태로 표기 (보드 카드·일정표·리포트 3곳)
- [x] Task 4-1 — `인증 통계` 탭: 모델×인증별 진행차수·Fail 횟수·Pass율·Fail율 (표 + 비율 바)
- [x] Task 4-2 — 주간보고에 `모델별 인증 현황` 섹션 분리 (해당 주차 월~금 기준)
- [x] 0 나눗셈 방어 (`pct()` 분모 0 → 0 반환) + 빈 DB 케이스 테스트
- [x] 집계 성능 — `idx_requests_model_cert` 인덱스 + SQL GROUP BY 집계 (앱단 Full Scan 제거)
- [x] `npm test` 스모크 31건 통과 + 전체 JS 구문검사 통과
- [x] 인증 통계에 주단위(월~금) 명시 — `주간` / `전체 누적` 토글 + `‹ 8/24~8/28 ›` 주 이동
- [x] 인증 통계 엑셀(CSV) 다운로드 — 화면에 보이는 기간 그대로, 대상 기간·합계 포함
- [x] 프런트 `weekRangeOf(0)` ↔ 서버 `workWeekRange()` 주 경계 일치 대조 통과
- [x] 라이브 서버 E2E 32건 통과 (등록→목록 노출→통계→주간보고→기존 회귀)
- [ ] Mac Mini 재배포 (`npm install` 후 재기동)
- [ ] 브라우저 UI 육안 확인 (모델명 드롭다운·진행차수 배지·통계 주 이동·엑셀 다운로드)

## 6차 고도화 (2026-08-26 구현 · 작업지시서 v2)
- [x] Task 1 — Test 목적 `MR` (5차에서 이미 구현, 변경 없음)
- [x] Task 2 — 모델명 자동 목록화 (5차에서 이미 구현, 변경 없음)
- [x] Task 3 — `진행차수 3차, Pass` 표기 (5차에서 이미 구현, 변경 없음)
- [x] Task 4-1 — `최근 Round 입력` 칼럼 삭제 + 미판정 건 집계 제외 (분모 = 판정 완료 건수)
- [x] Task 4-2 — `인증종류` 우측에 `결과`·`Test 목적` 칼럼 추가 (집계 단위를 Test 목적까지 분리)
- [x] Task 4-3 — `결과` 시각화: Pass 파란 볼드 / Fail 빨간 음영 + 흰 볼드 (화면·메일 양쪽)
- [x] Task 4 — 주간(월~금) 분리 표기 유지 + 주간보고 섹션 칼럼 동기화
- [x] Task 5 — `GET /api/next-round` 진행차수 자동 산출 (Fail → +1 / Pass·신규 → 1차)
- [x] Task 5 — `Round` select → number input 전환 (자동 기입 후 직접 수정 가능)
- [x] Task 5 — 비동기 조회 중 스피너 + 입력 잠금(중복 입력 방지) + 응답 순서 역전 방어
- [x] Task 6-1 — ⓘ 아이콘 → 진행차수 산출 근거 미니 타임라인 모달
- [x] Task 6-2 — 병목 경고 위젯 (반복 Fail 5차 이상 · 장기 미판정 14일 초과)
- [x] 0 나눗셈 방어 유지 (`pct()` 단일 지점)
- [x] CSV 다운로드 칼럼을 새 표에 맞춰 갱신
- [x] `npm test` 스모크 61건 통과 + 변경 파일 전체 구문검사 통과
- [x] 라이브 서버 E2E 30건 통과 (등록→차수 산출→통계→병목→주간보고 회귀)
- [x] 추가요청 — `Test 목적`에 직접입력 필드 (드롭다운 + `+ 직접 입력`, 테스터 콤보 패턴 재사용)
- [x] 추가요청 — 직접입력한 `Test 목적`이 다음부터 드롭다운에 자동 노출 (`/api/options` DISTINCT)
- [ ] Mac Mini 재배포 (`npm install` 후 재기동)
- [ ] 브라우저 UI 육안 확인 (차수 자동입력·스피너·ⓘ 타임라인·결과 색상·병목 위젯)

## 7차 보고 메일 (2026-08-26 구현 · 추가 요청)
- [x] 일일보고 — 매일 18:00 발송 (기존 19:00에서 변경)
- [x] 주간보고 — 매주 금요일 18:00 발송 (기존 매일 발송에서 변경)
- [x] 인증 통계 — 매주 월요일 09:00 단독 보고 신설 (대상: 지난주 월~금)
- [x] 세 보고 모두 엑셀(CSV) 첨부 — 화면 `⤓ 엑셀 다운로드`와 동일 서식(UTF-8 BOM)
- [x] 기존 메일 템플릿·서식 유지 (`shell()`로 겉틀 공용화, 인라인 스타일 그대로)
- [x] `notify.sendReportMail()`에 첨부 인자 추가
- [x] `/api/report/certstats` · `/api/report/certstats/send` 추가
- [x] 인증 통계 탭에 `✉ 지금 메일 발송` 버튼 추가
- [x] `npm test` 86건 통과 (요일·시각 계산, 첨부 BOM, 지난주 범위 포함)
- [ ] **config.json에 SMTP 입력** — 이것 없이는 실제 발송되지 않음 (사용자가 직접 작성)
- [ ] Mac Mini 재배포 후 상시 구동 확인 (launchd 미설정 시 서버가 꺼지면 발송 안 됨)

## 9차 버그 수정 + 본문 복사 (2026-08-26)
- [x] 일일·주간보고가 로딩 후 표시되지 않던 오류 수정 (`renderReport`의 미정의 변수 `p`)
- [x] 스케줄러 자동발송이 사내 상세 본문(`r.html`)을 보내던 결함 수정 → `r.mailHtml`
- [x] 스케줄러가 넘기는 인자를 가로채 검증하는 회귀 테스트 추가 (기존 테스트는 이 결함을 못 잡았다)
- [x] 일일·주간·인증통계 탭에 `📋 본문 복사` 버튼 추가 (메일 작성창에 서식 그대로 붙여넣기)
- [x] `GET /api/report/:period/copy` 신설 — certstats는 화면에서 고른 기간(`?from=&to=`) 반영
- [x] `report.certStatsCopy(range)` — 전체 누적(range=null)도 인라인 서식으로 생성
- [x] `npm test` 114건 통과
- [ ] **브라우저에서 복사 동작 육안 확인** — http(비보안 컨텍스트) 폴백 경로라 실제 브라우저 확인 필요
- [ ] Mac Mini 재배포

## 10차 진행차수 버그 수정 (2026-08-27 구현 · 작업지시서 v4 Task 0)
- [x] Task 1~6 — v4 문면이 v2와 동일 확인, 5·6차에서 이미 구현되어 변경 없음
- [x] Task 0 원인 분석 — `nextRound`/`roundHistory`가 모델명·Test 목적 2개만 비교하고 인증종류·Test type을 조건에서 빠뜨림 (지시서가 요구한 "4가지 조건 동시 매칭"과 불일치)
- [x] Task 0 원인 분석 — `prev.verdict === 'Fail'`만 검사해 이전 판정이 Drop이면 Pass처럼 취급(1차로 리셋), 이력 조회 자체에서도 Drop 건이 통째로 빠짐
- [x] Task 0 수정 — `roundHistoryOf`/`nextRoundOf`에 cert_type·test_type 조건 추가, 판정 조건을 `!== 'Pass'`로 변경(Fail·Drop 모두 이어짐), Drop도 이력에 포함
- [x] `GET /api/next-round`에 cert_type 필수 파라미터 추가, test_type 함께 전달
- [x] 프런트 — 인증종류/Test type 변경 시에도 자동 산출 재실행, 안내문구·ⓘ 모달에 4개 조건 노출
- [x] 스모크 테스트 회귀 5건 추가(인증종류 교차, Test type 교차, Drop 이어짐) + 기존 항목 4-인자 호출로 갱신 — `npm test` 121건 통과
- [x] 실 서버 기동 라이브 E2E 11건 통과 (400 검증, 신규 1차, Fail 이어짐, 인증종류/Test type 교체 시 리셋, Drop 이어짐, Pass 리셋) — 스크래치 스크립트, 저장소 미포함
- [x] Mac Mini 재배포 가이드 문서화 (`DEPLOY.md`) — PR #2 미병합 상태 반영, 백업·검증·롤백 포함
- [x] **추가 수정(같은 날, 사용자 재확인)** — 직전 건이 아직 판정 전(미판정/`-`)이어도 Pass가 아니므로 차수가 이어져야 하는데, `roundHistoryOf`가 미판정 건을 이력에서 통째로 제외해 그 앞의 오래된 판정을 잘못 짚고 있었다. `HAS_VERDICT` 필터 제거(미판정도 이력에 포함) + `nextRoundOf` basis에 `'pending'` 추가
- [x] 로컬 개발 서버(3001) 재기동해 수정 반영 확인 — 재기동 전 프로세스가 수정 전 코드로 떠 있어 브라우저 테스트가 구버전을 보고 있었음(원인의 일부)
- [x] `npm test` 123건 통과 (미판정 포함 회귀 2건 추가)
- [ ] Mac Mini 재배포 실행 (`DEPLOY.md` 따라 사용자가 직접, 기존 배포분은 이 수정 이전 버전)
- [ ] 브라우저 UI 육안 확인 (인증종류/Test type 바꿀 때, 그리고 직전 건이 미판정일 때 진행차수가 다시 산출되는지)

## 11차 QE 전체 리소스 현황 (2026-08-28 구현 · 작업지시서 v5 Task 6·7)
- [x] v5 Task 0~5 대조 — v4와 문면 동일, 10차까지 이미 완료되어 변경 없음
- [x] v5 Task 7 예시 2건(ⓘ 타임라인·장기체류 알림)도 6차 Task 6-1·6-2로 이미 구현됨 확인
- [x] `holidays.js` — 2026년 공휴일 상수 + `config.json` 오버라이드(add/remove) + 영업일 계산
- [x] `db.js openRequests()` — 예약대기·예약확정·진행중 건 조회 (리소스 산정 대상)
- [x] `resources.js` — Test type별 slot 규칙 + 담당자별 집계 (4명 / 미배정 / 기타)
- [x] `GET /api/resources` — 전체·담당자별 리소스 현황 (`?as_of=` 기준일 지정)
- [x] 신규 탭 `QE 리소스` — 요약 카드 8종 + 부하 막대(기준선 눈금) + 담당자별 표 + 건별 상세
- [x] 대시보드 상단 한 줄 요약 (현황 보드, 클릭 시 탭 이동 — `switchView()`로 경로 통합)
- [x] slot 규칙 미정의 조합 경고 표시 (조용히 0으로 세지 않는다)
- [x] 0 나눗셈 방어 — 빈 입력에서 기준선·소진일이 NaN/Infinity/지어낸 날짜가 되지 않음
- [x] 미등록 연도 경고 — 공휴일 상수에 없는 연도로 넘어가면 화면에 오차 안내
- [x] `npm test` 195건 통과 (123 → 195, Task 6 회귀 72건 추가)
- [x] 라이브 서버 E2E 33건 통과 (as_of 검증·등록→반영·완료 전환→제외·기존 API 회귀) — 스크래치, 저장소 미포함
- [x] Task 7 — 추가 기능 2건 기획 제안 (context-notes.md 11차 섹션)
- [x] 재수정 — `1주 가용(4명 × 5일 = 20 slot) = 100%` 기준으로 사용률·여유·초과 표기
- [x] 재수정 — 담당자별은 `1인 주간 가용 5 slot` 대비 여유/초과 (자동 할당이 채울 빈 slot 중심)
- [x] 재수정 — 쏠림 지표(초과 합계·쏠림 폭·균등분담선) 제거 (자동 할당으로 편중 해소 예정)
- [x] 신규 — `일별 가용 리소스 현황` (담당자별 직렬 큐 배치, 주 소계, 그날 여유 인원)
- [x] 신규 — 미배정 건을 가장 빨리 비는 담당자에 `배정 예정`으로 채우는 자동 할당 미리보기
- [x] 신규 — `?days=N` (5~60 영업일, 기본 20) + 구간 초과 물량 `overflow` 표기
- [x] 버그 수정 — 4명 외 테스터 물량이 일별 배치에서 조용히 사라지던 문제 (`daily.excluded`로 명시)
- [x] 불변식 테스트 — `배치분 + 구간초과 + 제외분 = 전체 물량`
- [x] 공휴일이 든 주의 가용 감소 검증 (추석 주 → 영업일 3일 × 4명 = 12 slot)
- [x] 기존 스케줄러 테스트 플레이키 수정 (생성 시각 의존) + 9차 결함 주입으로 목적 유지 확인
- [x] 재수정 2 — 기타 테스터를 **별도 리소스 풀**로 분리 (인증 담당 풀의 100%에 섞이던 왜곡 제거)
- [x] `dailyPlanOf`를 풀 단위로 일반화 (`members` · `absorbUnassigned`) → 두 풀의 일별 현황
- [x] 기타 풀은 자동 할당 대상 아님 (미배정 흡수하지 않음) · 기타 인원 0명이면 섹션 미표시
- [x] `rowOf` 가용 조건을 `!== 'unassigned'`로 (기타 테스터도 주간 가용 5 slot)
- [x] 불변식 2층 고정 — 풀별 `배치분 + 구간초과 = 풀 물량`, 전체 `두 풀 합 = overall`
- [x] `npm test` 266건 통과 / 라이브 E2E 70건 통과

### 11차-2 대시보드 5개 위젯 (2026-08-28 · 추가 명세)
- [x] **메인에는 `실시간 리소스 가동률`만** 노출, 나머지 4종은 QE 리소스 탭 (클릭 시 탭 이동)
- [x] 1 · 실시간 가동률 — 1일 총 가용(인원수) 대비 오늘 점유 확정업무, **잔여 가용 slot 강조**
- [x] 1 · `예약대기`는 가동률에서 제외 (lane에 `status` 실어 committed/waiting 분리)
- [x] 1 · 이번 주 보조 지표 + 80/100 색상 전환
- [x] 2 · 팀원별 부하도 — 개인별 slot 바 + 신호등(안정 ≤80% / 주의 ≤100% / 초과 >100%), 가용 눈금
- [x] 2 · `loadLevelOf()` 경계값 테스트 (80 · 80.1 · 100 · 100.1)
- [x] 3 · 파이프라인 — 3단계 건수·slot·담당미정 카드 + 단계별 slot 합 = 풀 물량 불변식
- [x] 3 · 가장 오래 대기 중인 건 최대 2건 (등록 후 대기일수, 희망일 경과 표시, 14일↑ 빨강)
- [x] 4 · 인증 타입별 점유 — 인라인 SVG 도넛 + 표 (외부 라이브러리 없음), 비중 합 100%
- [x] 5 · 스마트 알림 — 초과 할당 / 리소스 충돌 / 일정 밀림 / 리소스 확보 예정(D-N)
- [x] 5 · **충돌은 배치 전 '계획 일정'으로 판정** (직렬 큐는 겹침을 밀어내 배치 결과로는 못 잡는다)
- [x] 5 · 리스크 없으면 거짓 경보 만들지 않음 (clean 픽스처로 검증)
- [x] `npm test` 320건 통과 / 라이브 E2E 91건 통과

### 11차-3 가동률 100% 초과 표기 + 타임존 수정 (2026-08-28)
- [x] **배치 가동률은 100%를 넘을 수 없음 확인** (직렬 큐) → `demand`(계획 기준 수요) 신규
- [x] `day.demand` / `demand_pct` / `demand_over` / `doubled` — 계획 겹침 허용, 100% 초과 가능
- [x] `utilization.level` 서버 판정 — 계획 과부하 또는 여유 0이면 빨강
- [x] 막대에 100% 기준선 + 초과분 빗금 빨강
- [x] **타임존 결함 수정** — `toISOString()`(UTC)이 KST 00:00~09:00에 하루 뒤처짐
- [x] 영향 3곳 — `as_of` · 지연(overdue) 판정 · 장기 미판정 임계
- [x] UTC 경계 재현 회귀 테스트 (TZ=UTC / Asia/Seoul 양쪽 통과)
- [x] 거짓 과부하 방지 — 계획일 미입력 건수·slot 명시
- [x] 휴일 조회 안내 — `오늘은 휴무(추석) — 다음 영업일 기준`
- [x] `npm test` 351건 통과

### 11차-4 진행률 반영 (2026-08-28 · 고도화 2순위)
- [x] `holidays.businessDaysBetween(from, to)` — from 포함 / to 제외 (당일은 진행 중이라 소화 아님)
- [x] `remaining_slots` 컬럼 추가 (자동 마이그레이션) + ALLOWED/LABELS/create/update/openRequests
- [x] `progressOf()` — 자동 차감(시작일 기준) / 수동 보정 우선 / 미착수 전량
- [x] **예정 초과 시 잔여 최소 1 유지** (진행중 건이 조용히 사라지지 않게) + `alerts.overrun`
- [x] `item.slots`를 잔여로 전환, 계획은 `plan_slots`로 보존
- [x] 총계·담당자별·일별 배치·타입 분포 전부 잔여 기준
- [x] 의뢰 상세에 `잔여 slot` 입력 필드 (비우면 자동)
- [x] 건별 상세에 `계획 / 소화 / 잔여 / 진행률 바` + 산출 근거(수동·자동·미착수)
- [x] 풀 요약에 `진행률` 카드 + 담당자 행에 계획 대비 표기
- [x] 수동 보정 경계 검증 (상한·하한·0·비수치)
- [x] `npm test` 385건 통과 / 라이브 E2E 107건 통과 (PATCH → 응답 → 변경이력 → 원복)
- [ ] **공휴일 상수 검증** — 2026년 목록을 정부 공고와 대조 (사용자 확인 필요)
- [ ] 브라우저 UI 육안 확인 (탭 노출·상단 한 줄 요약 클릭·부하 막대·건별 상세 펼침)
- [ ] Mac Mini 재배포

## 12차 본문 복사 서식 유실 수정 (2026-08-31)
증상 — 일일보고·주간보고·인증통계의 `📋 본문 복사` 후 붙여넣으면 서식·템플릿이 하나도 살아나지 않음.
- [x] 서버 본문 점검 — 세 보고 모두 인라인 스타일, class·`<style>` 없음 (기존 테스트로 확인, 원인 아님)
- [x] 원인 — 클립보드에 text/html 이 실리지 않았다. 사내망 http 는 비보안 컨텍스트라 Clipboard API 가 막히고, 폴백이 `left:-99999px` 로 뷰포트 밖에 밀어 둔 요소를 선택 복사하는 방식이라 브라우저가 text/plain 만 싣는 경우가 있다
- [x] 원인 2 — 텍스트 대체본(`htmlToPlain`)이 화면에 붙지 않은 요소의 `innerText` 를 읽어 줄바꿈·표 구조가 통째로 사라진 한 덩어리였다
- [x] `copyViaCopyEvent()` 신설 — copy 이벤트를 가로채 `setData('text/html')`·`setData('text/plain')` 으로 실을 내용을 직접 지정 (http 에서 동작하는 기본 경로)
- [x] 복사·추출용 요소를 뷰포트 안(`left:0;opacity:0`)으로 옮김 — 화면에 붙어야 innerText·선택 직렬화가 제대로 동작
- [x] `htmlToPlain` 을 화면에 붙인 요소 기준으로 변경
- [x] Clipboard API 실패(권한 거부 등) 시에도 폴백으로 내려가도록 try 로 감쌈
- [x] 붙여넣는 쪽 인코딩 오해 방지로 `<meta charset="utf-8">` 를 html 앞에 붙임
- [x] 회귀 가드 3건 추가 — `npm test` 388건 통과
- [ ] **브라우저에서 복사 → 메일 작성창 붙여넣기 육안 확인** (사내망 http 경로. 자동 테스트로는 검증 불가)
- [ ] Mac Mini 재배포
### 12차-2 Outlook 데스크톱 서식 대응 (같은 날, 재확인)
표는 살지만 배지·칩 색이 죽는다는 재보고. 붙여넣기 대상은 Outlook 데스크톱.
- [x] 화면 HTML과 복사 HTML 대조 — 일일 3140자 / 주간 3593자로 **양쪽 완전 동일**. 우리 쪽 차이 아님을 먼저 확인
- [x] 원인 — Outlook 데스크톱은 Word 렌더러라 `span`·`b` 등 **인라인 요소의 `background`와 `border-radius`를 버린다**. 배지·칩이 전부 인라인 요소였다
- [x] `tag(text, bg, color, radius)` 신설 — 색 있는 조각을 1칸짜리 표(`display:inline-table`) + `bgcolor` 속성으로 감쌈
- [x] 판정 배지(`verdictBadge`)·통계 Fail 배지(`resultCell`)를 `tag()`로 전환
- [x] 요약 칩(`chip`/`summaryLine`)을 표 셀로 전환, 칩 간격은 Word가 무시하는 `border-spacing` 대신 스페이서 셀로
- [x] 표 머리행(`th`/`thNum`)에 `bgcolor="#f7f9fc"` 속성 추가
- [x] Fail 상세 박스를 `div` → 표로 전환 (왼쪽 빨간 띠는 4px 셀 + `bgcolor`)
- [x] 세 표에 `cellspacing="0" cellpadding="0" border="0"` 속성 추가 (Word는 style로 못 받음)
- [x] 회귀 가드 3건 추가 (bgcolor 존재 · 인라인 요소 background 없음 · cellspacing 속성) — `npm test` 391건 통과
- [ ] **Outlook 데스크톱에 붙여넣어 육안 재확인**
- [ ] Mac Mini 재배포

미해결(별건) — 인증통계는 화면(10칼럼 + 비율 막대 + 색상 배지)과 복사본(9칼럼, 비율 막대 없음)이
원래부터 다른 표다. 화면은 `styles.css` 클래스로, 복사본은 `report.js` 인라인 스타일로 따로 그린다.
"화면 그대로" 복사를 원하면 복사본을 10칼럼으로 다시 만들어야 한다.
### 12차-3 인증종류 배지 색 · 글자색 (같은 날, 재확인 2)
"Pass·Fail 글자색 미적용, NTS·xTS 색상 미적용".
- [x] 확인 — 복사본의 인증종류 칼럼은 **색 지정이 아예 없었다**(`<td …>Google xTS</td>`). 화면은 `styles.css`의 `.badge-netflix`/`.badge-google`로 칠하는데 서버 본문에는 대응물이 없었다
- [x] `CERT_COLORS` + `certKind()` + `certBadge()` 추가 — 화면 배지와 같은 색값(netflix `#fde8e8`/`#c0392b`, google `#e8f0fe`/`#1a73e8`, amazon `#fff1de`/`#c77700`)
- [x] 통계 표 인증종류 칼럼과 일일·주간 `인증 / Test` 칼럼에 배지 적용
- [x] `certOf()` — 배지와 Test 표기를 표 한 행 두 칸으로 묶음 (Word가 `display:inline-table`을 무시해 줄이 갈라지는 것 방지)
- [x] `fontColor()` 추가 — Word는 style의 `color`를 흘릴 수 있어 `<font color>` 속성과 이중 지정. Pass·Fail 숫자·Fail율·요약 칩 숫자·Fail 상세 제목·배지 안 글자에 적용
- [x] 기존 테스트 1건 갱신(마크업 고정 → 의도 검사) + 회귀 가드 3건 추가 — `npm test` 394건 통과
- [ ] **Outlook 데스크톱에 붙여넣어 육안 재확인**
- [ ] Mac Mini 재배포



## 향후(요청 시)
- [ ] 사용자 인증/로그인 (현재는 LAN 신뢰 기반, 인증 없음)
- [ ] Jira 티켓 링크 / 거증 첨부 (testrun 연계)
- [ ] 충돌·쿼터(테스트 슬롯 한정 시)
