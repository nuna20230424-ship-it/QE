# 컨텍스트 노트 — 인증 일정 대시보드

## 결정 사항
- **결과물**: 실제 동작하는 웹 대시보드 앱 (서브에이전트 정의 아님).
- **스택**: Node.js(Express) + 정적 프런트(빌드 단계 없음). 사용자는 GAS 경험이 풍부하지만 Mac Mini 서버를 별도로 제공해 자체 호스팅 선택.
- **DB**: better-sqlite3 (동기식, macOS/Windows 프리빌드 제공 → Mac Mini 네이티브 컴파일 부담 최소). 파일 DB `data.db`, WAL 모드.
- **인증**: 없음. 사내 LAN 신뢰 환경 가정. 역할(의뢰자/테스터)은 localStorage 토글일 뿐 보안 경계가 아님. 외부 노출 시 reverse proxy + 인증 필요.

## 데이터 모델 (requests)
- 의뢰자(개발 PL) 입력 영역: cert_type(넷플릭스/구글), model_name, fw_version, request_item, requester, note, desired_date(희망일정).
- 테스터 입력 영역: scheduled_date(예약확정), tester, status, progress, result.
- 상태 흐름: 예약대기 → 예약확정 → 진행중 → 완료 / 보류.

## UI 설계 의도
- **현황 보드**: 상태별 5컬럼 칸반. 한눈에 진행 분포 파악.
- **일정표**: 예약일(없으면 희망일) 정렬 테이블. 일정 공유 목적.
- 모달 한 폼에 의뢰 정보 / 진행·결과 두 fieldset. 역할에 따라 반대 영역 `disabled` 처리해 책임 분리.
- 신규 등록 시 진행/결과 영역은 비활성(의뢰 시점엔 불필요).

## 미해결 / 확인 필요
- **서버 IP**: `172.16.3.136`으로 확정 (2026-06-23). 운영 포트 `3001`, 접속 주소 `http://172.16.3.136:3001`.
- Mac Mini의 Node 버전 미확인. README에 Node 18+ 권장 명시.

## 지연(overdue) 정의 (2026-06-25 확정)
- 지연 = **예약 확정일(scheduled_date)이 오늘보다 이전인데 아직 `진행중`으로 전환되지 않은 건** (상태가 `예약대기`/`예약확정`로 잔류).
- 희망일(desired_date)은 지연 기준에서 제외. 진행중/완료/보류/중단 상태는 지연 아님.

## 현황보고 (2026-07-08 확정)
- **보드 접힘**: 각 상태 칼럼은 카드 5개까지만 노출, 나머지는 "+N개 더보기/접기" 토글. 펼침 상태는 클라이언트 메모리(state.boardExpanded)만 유지.
- **일일 기준**: 오늘 하루(완료일=오늘). **주간 기준**: 이번 주 월~일(사용자 확정). 완료 집계는 completed_date(없으면 completed_at 날짜) 기준.
- **진행중**: 기간과 무관하게 현재 status='진행중'인 건 전체를 표기(스냅샷 성격).
- **리포트 HTML**: report.js 한 곳에서 인라인 스타일로 생성 → 앱 뷰(#view-daily/weekly)와 이메일 본문에 동일 사용. 이메일 클라이언트가 <style>을 제거하므로 인라인 필수.
- **자동 발송**: scheduler.js가 매일 19시(SEND_HOUR) 일일+주간 2통 발송. setTimeout으로 다음 19시 재예약(상시구동 전제). 수신 기본값은 notify.js DEFAULT_REPORT_TO, config.reportTo로 재정의.
- **Fail 상세**: 완료&판정=Fail 건에 대해 진행사항(progress)+결과코멘트(result)를 별도 블록으로 첨부.

## 5차 고도화 (2026-08-26, 작업지시서 기준)

### 지시서 문면대로 두되 기록해 둘 판단 3건
- **`MR`이 Test type과 Test 목적 양쪽에 존재**. 지시서가 `Test 목적`에 추가하라고 위치를 명시해 그대로 넣었다. 원래 의도가 Test type이었다면 `index.html`의 `f-test_purpose`에서 빼면 된다.
- **`진행차수`의 정의가 Task 3과 Task 4에서 다르다.** Task 3은 기존 `round` 컬럼(1~5, 테스터 수동 입력), Task 4는 "누적 의뢰 횟수"(COUNT). 두 값을 통계 표에 `진행차수`(누적)와 `최근 Round 입력`(MAX) 두 칼럼으로 나란히 뒀다.
- **Pass율 분모는 '전체 의뢰 횟수'**(지시서 문면). 미판정 건이 분모에 남아 Pass율 + Fail율 < 100%가 된다. 통계 화면과 주간보고 섹션에 이 취지를 문구로 박아 오해를 막았다. 판정 완료 건만 분모로 쓰려면 `db.js`의 `pct(r.pass, r.total)`을 `pct(r.pass, r.pass + r.fail)`로 바꾸면 된다.

### 설계 결정
- **모델명 목록은 `state.items`가 아니라 서버에서 받는다.** `state.items`는 상단 필터가 걸린 목록이라 필터를 켜면 과거 입력값이 사라진다. "1번이라도 입력된 값 전체"라는 요건을 지키려면 `SELECT DISTINCT`가 필요해 `/api/options`를 새로 뒀다. 의뢰자 필드는 기존대로 `state.items` 기반이라 같은 한계가 남아 있다(요건 밖이라 미변경).
- **모델명에 DB UNIQUE 제약은 걸지 않았다.** 같은 모델을 여러 번 의뢰하는 게 정상이라 UNIQUE는 데이터 자체를 막는다. 중복 제거는 조회 시점의 `DISTINCT`로 처리한다.
- **집계는 SQL `GROUP BY`로 내린다.** 기존 `stats()`/`reportData()`처럼 전체 행을 앱으로 끌어와 순회하면 의뢰가 쌓일수록 느려진다. `certStats()`는 `idx_requests_model_cert (model_name, cert_type)` 커버링 인덱스를 타고 집계만 반환한다. `ORDER BY … COLLATE NOCASE`는 임시 B-TREE를 쓰지만 정렬 대상이 이미 그룹핑된 소수 행이라 영향이 없다.
- **"해당 주차에 진행된" 판정 기준**: 대표 시작일(`started_date` → `scheduled_date` → `desired_date`)과 대표 종료일(`completed_date` → 시작일 대체)이 만드는 구간이 주차와 겹치면 포함. 단일 기준일 방식은 주를 걸쳐 진행된 건을 놓쳐서 구간 겹침으로 갔다.
- **주간보고의 두 기간이 다르다.** 본문(완료/진행중 집계)은 기존대로 **월~일**, 새 인증 통계 섹션만 지시서 문면대로 **월~금**. 섹션 머리말에 기간을 명시해 혼동을 막았다. 기존 동작을 바꾸지 않으려는 선택이다.
- **0 나눗셈**은 `db.js`의 `pct(n, d)` 한 곳에서 막는다(`d > 0 ? … : 0`). 화면·리포트가 같은 값을 쓰므로 방어 지점이 하나다.
- **`DB_PATH` 환경변수**를 추가했다. 스모크 테스트가 운영 `data.db`를 건드리지 않게 하는 용도이고, 미설정 시 기존과 동일하게 `data.db`를 쓴다.

### 인증 통계 주단위 · 엑셀 (추가 요청)
- **주간을 기본 모드로 뒀다.** 이 탭이 주간업무보고를 쓰기 위한 화면이라 기본이 주간이어야 손이 덜 간다. `전체 누적`은 토글로 남겼다.
- **주 이동(`‹ ›`)을 넣은 이유**: 월요일에 지난주 실적으로 보고서를 쓰므로 이번 주에 고정하면 정작 필요한 주를 못 본다. `weekOffset`은 화면 상태일 뿐 저장하지 않는다(탭을 다시 열면 이번 주).
- **주 경계는 프런트·서버가 각자 계산한다.** `app.js`의 `weekRangeOf()`와 `report.js`의 `workWeekRange()`가 같은 규칙(월요일 시작, +4일)을 쓴다. 두 값이 어긋나면 화면과 메일 보고서가 다른 주를 가리키므로 테스트에서 대조한다.
- **집계 API는 그대로 재사용했다.** `/api/cert-stats?from=&to=`가 이미 기간을 받으므로 서버 변경이 없다. 주간/누적 차이는 쿼리스트링 유무뿐이다.
- **라벨 두 형식을 병기한다.** 주 이동 버튼 옆은 요청받은 짧은 형식(`8/24~8/28`), 그 아래 대상 기간 줄은 연도까지 있는 `2026-08-24 ~ 2026-08-28 (월~금)`. 짧은 형식만 두면 연말연시에 연도가 모호해진다.
- **엑셀은 기존 일정표와 같은 CSV+BOM 방식**을 쓴다. xlsx 라이브러리를 새로 넣지 않았다. 의존성이 늘고, 일정표 다운로드와 형식이 갈리며, 지금 필요한 건 Excel에서 열리는 표 하나뿐이다. `saveCsv()`로 공용화해 두 곳이 같은 코드를 쓴다.
- CSV에 대상 기간과 합계 줄을 넣었다. 파일만 떼어 봐도 어느 주 집계인지 알아야 보고서에 붙일 수 있다.

### 테스트
- `npm test` → `test/smoke.js` 31건. 빈 DB 0 나눗셈, DISTINCT 중복 제거, 모델×인증 분리 집계, 주차 필터(지난주 건 제외), 리포트 문자열까지 확인한다.
- 지시서에 없는 추가 산출물이다. 불필요하면 `test/`와 `package.json`의 `test` 스크립트를 지우면 된다.

## 6차 고도화 (2026-08-26, 작업지시서 v2)

### 지시서 v2가 5차 판단 2건을 뒤집었다
5차 때 "지시서 문면대로 두되 기록해 둔다"고 남긴 판단 중 두 개가 이번에 정정됐다. 기록을 남겨둔 덕에 어디를 바꿔야 하는지 바로 찾았다.
- **Pass율 분모** — 5차는 '전체 의뢰 횟수'(미판정 포함)라 Pass율 + Fail율 < 100%였다. v2 Task 4-1이 미판정 제외를 지시해 분모를 판정 완료 건수로 바꿨다. 이제 합이 100%다.
- **`최근 Round 입력` 칼럼** — 5차에서 '진행차수'(누적 COUNT)와 'Round'(수동 입력)의 정의 충돌을 두 칼럼으로 피해 갔다. v2가 이 칼럼 삭제를 지시해 정의를 하나로 합쳤다.

### 해석이 갈려 결정한 것
- **Task 4-2의 `결과` 칼럼이 집계 행과 맞지 않는다.** (모델 × 인증) 집계 행에는 결과가 여럿이라 단일 값이 안 나온다. 4-3이 행마다 Pass/Fail 하나를 요구하고 Task 4 본문은 Pass율 같은 집계 지표를 유지하므로, 건별 리스트가 아니라 **집계 단위를 (모델 × 인증 × Test 목적)으로 쪼개고 `결과`는 그 조합의 최신 판정**으로 냈다. Task 5가 (모델명, Test 목적)으로 차수를 계산하므로 단위가 맞아떨어진다.
- **`진행차수`를 최신 판정 건의 `Round`로 통일했다.** 누적 COUNT로 두면 Pass 후 1차로 리셋되는 Task 5 로직과 어긋나 같은 화면에서 두 숫자가 달라진다. `결과`와 `진행차수`가 같은 의뢰를 가리키게 해 표 한 줄이 "이 조합은 지금 몇 차에서 어떤 상태인가"를 답하도록 했다.
- **Drop 판정도 집계에서 뺐다.** 지시서는 '미판정'만 언급했지만 Drop을 분모에 남기면 Pass율 + Fail율이 다시 100%에 못 미친다. 인증 성패 통계에 넣을 성질도 아니라 `JUDGED = verdict IN ('Pass','Fail')` 한 곳에서 함께 걸렀다.
- **진행차수 계산 키에 인증종류를 넣지 않았다.** 지시서 Task 5가 "`모델명`과 `Test 목적`이 선택되는 순간"으로 키를 명시해 문면을 따랐다. **부작용** — Netflix NTS에서 3차 Fail이면 같은 모델·목적의 Google xTS 신규 의뢰도 4차로 뜬다. 실무상 어색하면 `db.js`의 `roundHistoryOf()` WHERE에 `AND cert_type = @cert`를 더하고 API·프런트에 `cert_type`을 넘기면 된다(통계 표는 이미 인증종류로 나뉘어 있어 그쪽은 그대로다). 자동값을 사용자가 고칠 수 있고 ⓘ에 근거가 보이므로 잘못된 값이 조용히 저장되지는 않는다.

### 설계 결정
- **최신 판정 건을 뽑을 때 `MAX(SORT_KEY)` + bare column을 썼다.** SQLite는 `min()`/`max()`가 정확히 하나일 때 bare column이 그 최댓값을 만든 행의 값을 갖도록 보장한다. 윈도우 함수나 상관 서브쿼리보다 짧고 인덱스도 그대로 탄다. 대신 `MAX()`를 하나 더 넣으면 조용히 깨지므로 주석으로 못 박았다.
- **정렬키는 `대표일자 || '#' || printf('%010d', id)` 문자열이다.** 같은 날 두 건이 있으면 나중 등록(id 큰 쪽)이 최신이 된다. 날짜를 `substr(...,1,10)`으로 잘라 `created_at`(ISO)과 `completed_date`(YYYY-MM-DD)가 섞여도 사전순 비교가 성립한다.
- **Test 목적 미입력 건은 `(미지정)`으로 정규화**해 한 그룹으로 묶었다(`PURPOSE`). NULL과 빈 문자열이 각각 다른 행으로 갈라지는 것을 막는다.
- **`nextRound` 응답에 `history`를 함께 실었다.** ⓘ 타임라인이 쓸 데이터가 차수 계산에 쓴 것과 같아서, 따로 API를 두면 같은 질의를 두 번 하고 두 화면이 어긋날 여지가 생긴다.
- **자동 산출 응답 경쟁 조건을 `seq` 카운터로 막았다.** 모델명을 빠르게 고쳐 타이핑하면 이전 요청이 나중에 도착해 최신 값을 덮을 수 있다. 요청마다 번호를 매겨 최신 번호가 아니면 결과를 버린다. 조회 중에는 입력을 `readOnly`로 잠가 중복 입력도 막는다(지시서 주의사항).
- **`readOnly`를 썼지 `disabled`가 아니다.** `disabled`는 역할 잠금(`fieldset:disabled`)과 겹쳐 해제 시점이 꼬인다.
- **사용자가 차수를 직접 고치면 `roundAuto.touched`가 서고, 이후 자동값이 덮지 않는다.** 지시서가 수동 보정을 명시했는데 매번 덮어쓰면 보정이 불가능하다. 힌트 문구에 "직접 입력한 값을 유지합니다"를 붙여 왜 안 바뀌는지 보이게 했다.
- **기존 의뢰 상세를 열 때는 자동 산출을 돌리지 않는다.** 저장된 차수가 조용히 바뀌면 이력이 왜곡된다.
- **병목 임계값은 5차 / 14일.** 5차는 지시서 예시("5차 이상 연속 Fail")를 그대로 가져왔고, 14일은 정한 값이라 `?round=&days=` 쿼리로 조정할 수 있게 뒀다. **Pass로 끝난 조합은 반복 Fail 경고에서 뺐다** — 이미 해결된 건을 리스크로 띄우면 위젯을 안 보게 된다.
- **병목 위젯은 현황 보드에서만 뜨고 대상이 없으면 통째로 숨는다.** 항상 자리를 차지하면 경고로서 눈에 안 띈다.
- **병목 목록의 장기 미판정 건은 `/api/requests/:id`로 직접 조회해 연다.** 상단 필터가 걸려 있으면 `state.items`에 없어 클릭이 죽는다.

### Test 목적 직접입력 (추가 요청)
- **테스터 콤보 패턴(`setCombo`/`readCombo`/`bindCombo`)을 그대로 재사용했다.** 모델명·의뢰자가 쓰는 datalist 방식도 있었지만, 요청이 "별도 입력할 수 있는 입력필드도 추가로"라 드롭다운을 남기고 옆에 입력칸을 여는 쪽이 문면에 맞고 테스터 필드와 조작법이 같아진다.
- **`test_purpose`를 `F`(1:1 필드 맵)에서 빼고 콤보 헬퍼로 옮겼다.** `F`는 `.value`를 그대로 읽고 쓰는 필드만 담는 구조라 콤보를 섞으면 저장 시 select 값(`__custom__`)이 그대로 들어간다.
- **`purposeValue()` 한 곳으로 읽는다.** 진행차수 캐시 키·산출 질의·근거 모달 세 곳이 같은 값을 봐야 한다. select 값을 직접 읽던 코드가 남으면 직접입력한 목적으로는 차수가 안 잡힌다.
- **자동 산출 트리거를 select의 `change`와 입력칸의 `input` 양쪽에 걸었다.** `+ 직접 입력`을 고른 직후는 값이 비어 있어 타이핑까지 기다려야 차수가 나온다. 입력칸은 모델명과 같은 250ms 디바운스를 쓴다.
- **직접 입력한 목적은 다음 의뢰부터 드롭다운에 자동으로 뜬다(추가 요청으로 구현).** `/api/options`가 `testPurposes`를 함께 내려주고 `buildPurposeOptions()`가 모달을 열 때 끼워 넣는다.
- **HTML의 기본 옵션은 그대로 두고 동적 항목만 `data-dyn`으로 표시해 매번 제거·재삽입한다.** select를 통째로 다시 그리면 기본 목록을 JS에도 중복 정의해야 하고, 두 곳이 어긋나면 옵션이 조용히 사라진다.
- **동적 항목은 기본 목록 뒤, `+ 직접 입력` 앞에 넣는다.** `+ 직접 입력`이 중간에 끼면 목록의 끝이 어딘지 읽기 어렵다.
- **`buildPurposeOptions()`는 `setCombo()`보다 먼저 부른다.** `setCombo`는 값이 옵션에 있는지로 드롭다운 선택과 직접입력칸을 가르므로, 순서가 뒤집히면 이미 목록에 있는 값도 직접입력칸으로 열린다.
- **모델명과 달리 `test_purpose`에는 인덱스를 붙이지 않았다.** 값 종류가 예닐곱 개뿐이라 인덱스를 타도 스캔과 차이가 없고, 유지 비용만 는다. 담당 테스터는 요청 범위 밖이라 기존 방식(누적 없음) 그대로 뒀다.

### 검증
- `npm test` → 스모크 61건 (5차 31건에서 확장). 미판정 제외, Test 목적 분리, 최신 판정 선택, 차수 산출 4케이스, 병목 임계값, 리포트의 Pass/Fail 인라인 스타일까지 확인한다.
- 라이브 서버 E2E 30건 — 임시 DB로 서버를 띄워 등록 → 1차 → Fail → 2차 → Pass → 1차 리셋 흐름과 신규 API 응답 형태, 400 검증, 주간보고 회귀를 실제 HTTP로 확인했다. 스크래치 스크립트라 저장소에는 넣지 않았다.
- 병목 테스트 픽스처는 주차가 아니라 `daysAgo(40)` / `daysAgo(20)`로 고정했다. 주차 기준으로 잡으면 오늘이 무슨 요일인지에 따라 결과가 흔들린다.

## 7차 보고 메일 (2026-08-26, 추가 요청)

### 결정
- **인증 통계 보고(월요일 09:00)의 대상은 '지난주' 월~금.** 월요일 아침엔 이번 주 실적이 비어 있어 이번 주로 잡으면 빈 표가 나간다. 화면의 주 이동(`‹ ›`)을 넣을 때 적은 이유("월요일에 지난주 실적으로 보고서를 쓴다")와 같은 판단이다. `lastWorkWeekRange()`로 분리했다.
- **엑셀은 새 라이브러리 없이 CSV+BOM으로 냈다.** 요청이 "기존 템플릿·서식 동일 유지"였고, 화면의 `⤓ 엑셀 다운로드`가 이미 이 형식이다. xlsx 라이브러리를 넣으면 의존성이 늘고 화면 다운로드와 형식이 갈린다. 진짜 `.xlsx`가 필요하면 그때 바꾼다.
- **주간보고 첨부는 2개다.** 본문이 월~일 현황 + 월~금 인증통계 두 부분이라 한 파일에 섞으면 기간이 다른 표가 붙는다. 파일을 나눠 각각의 기간을 파일명에 박았다.
- **일일보고 첨부는 본문 표와 같은 항목으로 맞췄다.** 일정표 CSV(`downloadExcel`)와 칼럼이 다른데, 보고 메일 첨부는 그 메일 본문을 그대로 옮긴 것이어야 읽는 사람이 헷갈리지 않는다.

### 설계
- **`buildHtml`의 겉틀을 `shell()`로 뽑았다.** 인증 통계 단독 보고가 같은 머리말·꼬리말 서식을 써야 하는데, 스타일 문자열을 복사하면 한쪽만 고쳐질 때 두 메일의 서식이 갈린다.
- **`certStatsSection(now, range)`에 range 인자를 더했다.** 기본값은 기존 동작(이번 주 월~금)이라 주간보고는 그대로고, 월요일 보고만 지난주를 넘긴다.
- **스케줄러를 잡(job) 목록으로 바꿨다.** 기존은 "매일 19시에 2통"이라 요일 개념이 없었다. `days`(getDay 기준)를 두고 `msUntilNext()`가 시각과 요일을 함께 맞춘다. `days: null`이면 매일이다.
- **잡마다 개별 `setTimeout`을 걸고 발송 후 재예약한다.** 한 타이머로 세 잡을 묶으면 시각이 다른 잡(9시/18시)을 다루기 어렵다.
- **`msUntilNext()`를 export했다.** 요일·시각 계산이 이번 변경에서 가장 틀리기 쉬운 부분이라 고정 시각을 넣어 테스트한다(수요일에 본 주간보고 → 금요일, 금 19시에 본 주간보고 → 다음 주 금요일).
- **`/api/report/:period`의 period 허용값을 맵으로 뺐다.** 조회·발송 두 라우트가 같은 검증을 중복하고 있었고 여기에 certstats가 더해지면 세 곳이 어긋난다.

### 미완 (사용자 작업 필요)
- **`config.json`이 없어 실제 발송은 되지 않는다.** SMTP 사용자/앱 비밀번호는 비밀이라 대화·저장소에 넣지 않는다. `config.example.json`을 복사해 사용자가 직접 채워야 한다. 채우기 전까지 앱·스케줄러는 정상 동작하고 발송만 건너뛴다(`sent: false`).
- 수신자 기본값(`nuna20230424@gmail.com`, `keonhee.cho@kaongroup.com`)은 이미 요청과 같아 코드 변경이 없었다. **→ 8차에서 개인 Gmail을 제거했다(아래 참조).**

## 9차 버그 수정 + 본문 복사 (2026-08-26)

### 버그 원인
- **일일·주간보고 미노출**: `renderReport(period)` 안의 안내 문구가 정의되지 않은 `p`를 참조했다. `p`는 `buildQuery()`의 지역변수라 이 스코프엔 없다. 템플릿 리터럴 평가 중 ReferenceError가 나면서 `root.innerHTML = ...` 대입 자체가 중단됐고, 직전에 넣은 "불러오는 중…"이 그대로 남았다. `try/catch`가 fetch만 감싸고 있어 이 예외는 잡히지 않았다. **교훈은 렌더 함수 전체를 감싸는 게 아니라, 화면에 넣을 문자열을 만든 뒤에 대입하는 순서 자체가 취약하다는 점이다.**
- **스케줄러 상세 본문 발송**: 8차에서 메일을 링크 전용으로 바꿀 때 `server.js`의 수동 발송 라우트만 `mailHtml`로 고치고 `scheduler.js`의 `runJob`이 누락됐다. 결과적으로 버튼 발송은 안전한데 자동발송만 실명이 나갔다. 없어진 `r.attachments`를 셋째 인자로 계속 넘기고 있던 것도 같은 누락의 흔적이다.
- **테스트가 못 잡은 이유**: 8차 테스트는 `report.js`가 만드는 `mailHtml`만 검증했다. 산출물이 옳아도 **호출부가 다른 걸 넘기면** 통과한다. 그래서 이번엔 `notify.sendReportMail`을 가로채 스케줄러가 실제로 넘긴 인자를 본다. 되돌려 놓고 돌려 9건이 FAIL하는 것도 확인했다.

### 결정
- **복사 본문은 화면용 상세(`r.html`).** 사용자 선택이다. 사내 수신자에게 직접 메일을 쓰기 위한 기능이라 집계+링크만으로는 쓸모가 없다. 다만 8차에서 막은 실명 유출을 사람 손으로 되살릴 수 있는 경로라, 버튼 옆 안내 문구에 "사내 수신자에게만"을 박았다. **자동발송(mailHtml)과 복사(html)의 용도가 갈린다는 점이 이 기능의 핵심 위험이다.**
- **`navigator.clipboard`가 아니라 execCommand 폴백이 사실상 기본 경로다.** 대시보드는 `http://172.16.3.136:3001`로 열리는데 Clipboard API는 보안 컨텍스트(https·localhost)에서만 동작한다. `window.isSecureContext`로 먼저 갈라 두고, 화면 밖 요소를 선택해 복사하는 경로를 항상 살려 둔다. 사내망을 https로 바꾸기 전까지 이 폴백을 지워선 안 된다.
- **복사 HTML은 서버가 만든다.** 통계 탭 화면은 `styles.css`의 class로 그려져 있어 그대로 복사하면 메일에서 서식이 다 날아간다. `report.js`의 인라인 스타일 본문을 받아 쓰면 자동발송 메일과 서식이 한 곳에서 관리된다. 테스트로 `class=` 부재 · `style=` 존재 · `<style>` 블록 부재를 검사한다.
- **`GET /api/report/:period/copy` 하나로 세 보고를 다 받는다.** 일일·주간은 이미 화면에 있는 `.report-body`를 그대로 복사해도 되지만, 통계만 서버를 타면 경로가 둘로 갈린다. 한 경로로 두면 복사 시점의 최신 데이터가 보장되는 이점도 있다.
- **`statsBlock(s, label)`로 머리말을 뽑되 라벨 문구는 인자로 받는다.** 주간보고의 "대상 주차 …" 출력이 한 글자도 바뀌지 않도록 하기 위해서다. 전체 누적은 "대상 기간 전체 누적"이 된다.

### 미완
- **브라우저 육안 확인이 남았다.** 클립보드는 Node 테스트로 검증할 수 없다. 서버가 주는 HTML(서식·기간·상세 포함 여부)까지는 테스트가 보증하지만, 실제 붙여넣기 결과는 사람이 확인해야 한다.

## 10차 진행차수 버그 수정 (2026-08-27, 작업지시서 v4)

### v4가 실제로 요구한 것
v4의 Task 1~6은 v2와 문면이 동일해 5·6차에서 이미 끝나 있었다. 새로 요구된 것은 맨 앞의 **Task 0** 하나뿐이다 — "판정이 Pass가 아니면 인증종류·Test type·Test 목적·모델명 4가지가 모두 같을 때 차수가 이어져야 하는데 정상 동작하지 않는다"는 버그 리포트.

### 원인 (코드 대조로 확인)
6차에서 구현한 `roundHistoryOf`/`nextRoundOf`(`db.js`)가 실제로는 **모델명 + Test 목적 2개만** 비교하고 있었다. 이는 6차 context-notes에 "진행차수 계산 키에 인증종류를 넣지 않았다"고 이미 기록해 둔 **의도된 설계 결정**이었는데(지시서 Task 5 문면을 그대로 따른 것), v4가 이 부작용을 정식으로 "오작동"이라 지적하며 뒤집었다. `[[qe-work-order-flow]]`에 남긴 패턴대로 판단을 기록해 둔 덕에 정정 지점을 바로 찾았다.

두 번째 원인은 별개 버그였다. `nextRoundOf`가 `prev.verdict === 'Fail'`만 검사해 **Drop을 Pass와 동일하게 취급**(1차로 리셋)했고, `roundHistoryOf`가 쓰던 `JUDGED`(Pass/Fail만)를 그대로 재사용해 **Drop 건은 이력 조회에서 통째로 빠졌다**. 지시서 문면 "Pass가 아닌 경우(Fail 등)"은 Drop도 포함하는 부정 조건인데 긍정 조건(`=== 'Fail'`)으로 짜여 있던 것이 논리 오류였다.

### 수정
- `db.js`: `JUDGED`(통계 전용, Pass/Fail만)와 별도로 `HAS_VERDICT`(판정이 있으면 전부, Drop 포함)를 두어 통계 집계 로직(4-1)에 영향 없이 진행차수 계산만 고쳤다.
- `roundHistoryOf(modelName, testPurpose, certType, testType)` — cert_type·test_type을 WHERE에 추가.
- `nextRoundOf`의 continuation 조건을 `prev.verdict !== 'Pass'`로 바꿔 Fail·Drop 모두 이어지게 했다. `basis`는 `'fail'`/`'drop'`으로 구분해 두어 UI가 필요하면 구분 표시할 수 있게 했다(현재는 `reason` 문구만 씀).
- `GET /api/next-round`에 `cert_type`을 **필수 파라미터**로 추가했다(`model_name`과 동급 400 검증). test_type은 UI에 항상 값이 있는 select라 필수로 막지 않았다.
- 프런트(`app.js`): `roundKeyOf()`·조회 쿼리·ⓘ 모달 표시에 인증종류·Test type을 추가하고, 두 필드의 `change` 이벤트에도 자동 산출을 걸었다(기존엔 모델명·Test 목적 변경에만 반응).

### 판단 — cert_type 없이 호출하면 어떻게 되는가
`nextRoundOf`에 cert_type을 안 넘기면(문자열 `''`) 실제 데이터의 cert_type은 NOT NULL이라 매칭되는 행이 없어 자동으로 "이력 없음 → 1차"가 된다. 서버 라우트에서 400으로 막았지만, db 계층도 조용히 틀린 값을 주지 않고 안전하게 실패하도록 별도 테스트로 확인해 뒀다.

### 판단 — 통계 화면(Task 4)은 그대로 둔다
`certStatsOf`는 여전히 (모델명 × 인증종류 × Test 목적)로만 묶고 Test type은 구분하지 않는다. Task 0은 진행차수 **산출**(Task 5) 버그이지 통계 **집계**(Task 4) 요건이 아니고, v4도 Task 4 문면을 바꾸지 않았다. 다만 이 때문에 같은 (모델·인증종류·Test목적) 조합에 Test type이 여러 개면 통계 표의 "진행차수"(최신 판정 건 Round)가 자동산출 로직이 보는 차수와 다를 수 있다 — 기존에도 있던 한계이고 이번 지시서 범위 밖이라 그대로 뒀다.

### 검증
- `npm test` 121건(기존 114건에서 7건 추가 — 인증종류 조건 회귀 2건 + Task 0 신규 블록 5건). 회귀용 픽스처(KM-700/800/900)는 인증종류·Test type 교차 오염과 Drop 이어짐을 확인한 뒤 곧바로 `repo.remove()`로 지워 이후 통계·병목·리포트의 정확한 건수 비교(`총계 판정 건수 = 7` 등)에 영향이 없게 했다.
- 실 서버(`server.js`)를 임시 포트로 띄운 라이브 E2E 11건 — 400 검증, 신규 1차, Fail 이어짐, **인증종류를 바꾸면 1차로 돌아가는지**, **Test type을 바꾸면 1차로 돌아가는지**, Drop 이어짐, Pass 리셋을 HTTP로 직접 확인했다. 스크래치 스크립트라 저장소에는 넣지 않았다.

### 추가 수정 — 미판정(직전 건이 아직 판정 전)도 차수를 이어야 함 (같은 날, 사용자 재확인)
같은 대화에서 사용자가 "Pass만 조건으로 걸고 Fail·`-`(미판정)·Drop은 모두 진행차수로 적용되어야 함"이라고 다시 지적했다. 코드를 대조해 보니 두 가지가 겹쳐 있었다.

1. **로컬 서버가 수정 전 코드로 떠 있었다.** 3001번 포트 프로세스가 2026-08-26 14:47에 시작돼, 오늘 05시경 고친 `db.js`/`server.js`/`public/app.js`를 반영하지 못한 채였다(Node는 파일 변경을 자동 반영하지 않는다). 3000번 포트는 이 프로젝트와 무관한 다른 Next.js 프로세스(`C:\GTVS\dashboard\web`)였다 — `netstat`+`Get-CimInstance Win32_Process`로 커맨드라인을 대조해 확인. 사용자 승인 후 3001만 재시작했다.
2. **진짜 코드 버그가 하나 더 있었다.** `roundHistoryOf()`가 `HAS_VERDICT`(`verdict IS NOT NULL AND TRIM(verdict) <> ''`) 조건으로 미판정 건을 이력 조회 자체에서 뺐다. 그 결과 "가장 최근 건"이 아직 판정 전(진행중·예약확정 등, verdict 빈값)이면 그 건은 이력에서 통째로 사라지고, `nextRoundOf`는 그보다 더 오래된 판정 건을 `prev`로 잘못 짚어 차수를 계산했다 — 화면상 "왜 안 바뀌지"로 보이는 근본 원인.

**수정**: `HAS_VERDICT` 필터를 제거해 `roundHistoryOf`가 (모델명·Test 목적·인증종류·Test type이 일치하는) 모든 건을 verdict 유무와 무관하게 이력에 담게 했다. `nextRoundOf`의 계속 조건은 이미 `prev.verdict !== 'Pass'`라 미판정(빈 문자열)도 자동으로 포함된다. `basis`에 `'pending'`을 추가하고, `reason` 문구가 빈 verdict를 그대로 보간해 "판정이 이므로"처럼 어색해지지 않도록 `미판정` 라벨로 치환했다.

**판단 — 통계(`certStatsOf`)는 손대지 않았다.** `JUDGED`(Pass/Fail만)는 그대로 두었다. 미판정을 진행차수 산출의 "이전 건"으로는 인정하되, Pass율/Fail율 집계 분모에 넣지 않는다는 기존 원칙(Task 4-1)과는 별개 문제다.

**테스트**: `npm test` 121→123건. 기존 Task 5 블록의 KM-100(1차 Pass·3차 Fail·4차 진행중) 픽스처가 미판정 포함 여부를 정확히 가르는 케이스라, 그 위에서 기대값을 갱신했다(직전 미판정 4차 → 5차, basis `pending`, history 2건→3건). Task 0 블록에 KM-950(직전 미판정 4차 → 5차) 회귀를 별도로 추가했다.

## 11차 QE 전체 리소스 현황 (2026-08-28, 작업지시서 v5)

### v5가 실제로 요구한 것
v5의 Task 0~5는 v4와 문면이 같아 10차까지 이미 끝나 있었다. Task 7(추가 기능 제안)에 예시로 적힌 두 가지(ⓘ 진행이력 타임라인 · 장기체류 대시보드 알림)도 6차에서 각각 Task 6-1·6-2로 구현됐다. 새로 요구된 것은 **Task 6 — QE 전체 리소스 현황 대시보드** 하나이고, Task 7은 예시가 소진됐으니 새 항목을 기획해야 한다.

### 리소스 모델 — 지시서가 정하지 않은 것을 사용자와 확정했다
지시서는 `1 slot = 1명의 1일 업무량`과 Test type별 소요 slot만 준다. 그런데 slot이 person-day 단위라 **"전체 가용 리소스"는 기간을 정하지 않으면 정의되지 않는다** — NTS 1건이 12 slot인데 4명의 1일 가용은 4 slot이므로 "20% 사용" 같은 비율이 나올 수가 없다. 사용자 확인으로 다음을 확정했다.

- **기간 개념을 두지 않는다.** 미완 건의 총 slot을 그대로 쌓고, 그것을 **소요 영업일수**로 환산해 보여준다. `1 slot = 1명 1일`이므로 한 사람에게 할당된 slot 합계가 곧 그 사람의 소요 영업일수다(변환 계수 1). 주간/월간 토글을 두지 않은 것은 사용자 선택이다 — 주 단위로 자르면 NTS 12 slot 한 건이 특정 주에 240% 초과로 찍혀 판단에 방해가 된다.
- **영업일은 주말 + 공휴일을 제외한다.** 사용자 요구사항이다(지시서에는 없다).
- **여유/초과는 4명 균등분담 기준선 대비 편차로 정의한다.** 기간이 없으니 "가용 대비 사용률"을 담당자별로 낼 수 없다. 대신 `전체 slot ÷ 4`를 기준선으로 두고 각자의 초과·여유를 낸다. 리더가 재배정할 때 그대로 쓰이는 숫자라 지시서의 "담당자별 여유/초과 리소스 현황" 요구를 실질적으로 충족한다.

### 추가 수정 — 전체를 100%로 정규화한 정량 표기 (같은 날, 사용자 재요청)
사용자가 "전체 리소스를 100으로 산정해서 % 초과·% 여유가 있는지 정량적으로 표기", "담당자별 리소스 할당은 세부내용으로 분류", "써머리에 최다부하 담당자는 미표기"를 요청했다.

**분모를 무엇으로 잡을지가 유일한 쟁점이었다.** 기간 개념을 두지 않기로 이미 정했으므로 "4명의 총 처리 능력"이라는 가용 분모는 존재하지 않는다(시간이 지나면 언젠가 다 처리되므로 무한이다). 자체 정합성이 있는 분모는 **전체 미완 물량** 하나뿐이다. 그래서 이렇게 잡았다.

- **전체 미완 물량 = 100%.** 담당자별 `share`(점유율)의 합이 100%가 된다(4명 + 미배정 + 기타).
- **균등분담선 = `100 ÷ 인원수` = 25%** (물량과 무관하게 인원수로만 정해진다).
- **초과/여유 = `share − 25`(%p).** 양수면 초과, 음수면 여유. **옮겨야 할 slot 수를 함께 표기**해 재배정에 바로 쓰게 했다.
- 전체 레벨 지표로 **`초과 합계`**(기준선을 넘긴 담당자들의 초과분 합, %p)와 **`쏠림 폭`**(최다−최소 점유율)을 새로 뒀다. 전체가 항상 100%라 전체 레벨에서는 "초과/여유"가 성립하지 않는데, `초과 합계`는 **재배정으로 옮겨야 하는 물량의 크기**라는 실질적 의미를 갖는다.

**대안(1주 가용 = 4명 × 5일 = 20 slot을 100%로)은 택하지 않았다.** 숫자는 더 직관적이지만 "1주"라는 임의 상수를 새로 끌어들이고, 사용자가 앞서 주간 기준을 명시적으로 거부한 것과도 어긋난다. 지금 방식은 모든 수치가 데이터에서 도출된다.

**물량이 0이면 편차도 0으로 둔다.** 초기 구현은 `share(0) − 25 = −25`로 계산해 일이 하나도 없는데 "25%p 여유"가 떴다. 쏠림이라는 개념 자체가 없는 상태라 오해를 준다. `hasWork` 가드를 넣고 테스트로 고정했다.

**화면 구조를 바꿨다.** 상단은 전체 100% 기준 정량 지표 8종 + 합이 100%인 배분 바만 두고(담당자 이름 없음), 담당자별 할당은 `세부내용 · 담당자별 리소스 할당` 섹션으로 내렸다. 현황 보드 상단 한 줄 요약에서도 **최다 부하 담당자 표기를 제거**하고 전체 % 지표(균등분담선·초과 합계·쏠림 폭·미배정 %)로 대체했다.

### 부수 발견 — 기존 스케줄러 테스트가 플레이키였다
Task 6 테스트를 90건 이상 추가하자 `스케줄러 자동발송 본문` 3건이 간헐적으로 FAIL했다. 원인은 내 변경이 아니라 **기존 테스트의 시각 의존성**이다. `report.js`의 본문 푸터에는 `자동 생성 · <생성 시각>`이 `now.toLocaleString('ko-KR')`로 박히는데, 테스트는 파일 앞에서 만들어 둔 기대값과 스케줄러가 발송 시점에 다시 만든 본문을 **문자열 전체로** 비교한다. 두 시점 사이에 초가 넘어가면 깨진다. 실행이 길어질수록 자주 걸리므로 원래도 잠재된 결함이었다.

검증 의도는 "어느 본문(mailHtml vs 화면용 html)을 보냈는가"이므로 **생성 시각만 지우고 비교**하도록 고쳤다(`stripTs`). 시각을 비교 대상에서 빼도 9차에서 이 테스트를 만든 목적(스케줄러가 `r.html`을 넘기면 잡아낸다)은 그대로 유지된다 — 되돌려 놓고 돌려 실패하는 것도 확인했다.
- **미배정(tester 빈값)은 별도 행으로 분리**하되 전체 합계에 포함한다. 배정해야 할 물량을 숨기지 않는다.
- **4명 외 테스터(김지윤·조진원 등)는 `기타` 그룹으로 표시**한다. 지시서는 4명만 명시하지만 DB에는 다른 이름이 있고, 그 건을 화면에서 빼면 전체 합계가 실제 물량과 어긋난다.

### slot 규칙 — 지시서 표에 없는 조합을 어떻게 다루는가
지시서 표는 `xTS (IR, LR)=3`, `xTS (MR, 파생)=2`, `NTS (전부)=12`, `ATVS=4`다. DB의 `cert_type`은 `Google xTS` · `Netflix NTS` · `Amazon AVTS` 세 값이고, 지시서의 `ATVS`는 **`Amazon AVTS`의 표기 오차로 판단**했다(인증 3종 중 나머지가 하나뿐이고 slot 값도 별도로 주어졌다).

- xTS는 **`MR`·`파생`만 2로 하고 나머지는 전부 3**으로 잡았다. 구 데이터에 `test_type`이 빈 건이 있을 수 있는데, 이때 낮은 값(2)을 주면 물량이 과소평가된다. UI 기본값이 `IR`이라는 점과도 맞는다.
- **규칙에 없는 조합은 0으로 세지 않고 `미정의`로 따로 모아 화면에 경고한다.** 조용히 0 slot으로 처리하면 전체 물량이 틀리는데 아무도 모른다. 인증종류는 서버가 3종으로 검증하니 실질적으로 비어야 하는 목록이고, 비어 있지 않다면 그게 신호다.

### 공휴일 — 상수 + config 오버라이드, 그리고 검증은 사람이 한다
사내망이라 상시 외부 API 호출은 배제하고 `holidays.js` 상수로 둔다. `config.json`의 `holidays.add`/`holidays.remove`로 사내 창립기념일이나 정상근무일을 운용자가 덮어쓴다.

**목록의 정확성은 코드가 보증하지 못한다.** 음력 기반 공휴일(설·추석·부처님오신날)과 대체공휴일은 정부 공고로 확정되는 값이라, 상수에 넣은 2026년 목록은 사용자가 공고와 대조해야 한다. 그래서 `holidays.js` 주석과 README에 대조 필요를 명시하고, **등록되지 않은 연도로 예상일이 넘어가면 화면에 경고를 띄운다**(주말만 제외한 값이라 오차가 있다는 뜻). 지어낸 날짜를 확정값처럼 보여주지 않기 위한 장치다.

### Task 7 — 추가 기능 제안 (미구현, 승인 시 착수)
v5 Task 7에 예시로 적힌 두 가지는 6차에서 이미 구현됐다(ⓘ 진행이력 타임라인 = Task 6-1, 장기체류 대시보드 알림 = Task 6-2). 그래서 11차에서 만든 리소스 모델 위에 얹을 수 있는 새 항목 두 개를 제안한다. 둘 다 **기존 데이터만으로 계산되고 새 입력을 요구하지 않는다.**

**제안 1 — 의뢰요청 폼의 `착수 가능일 제안`**
의뢰요청 모달에서 인증종류·Test type이 정해지는 순간 그 건의 소요 slot이 확정된다(예: NTS = 12 slot). 여기에 4명 각자의 현재 큐를 더하면 **"누가 언제 착수할 수 있는지"가 그 자리에서 나온다.**

- 진행차수 자동 산출과 같은 위치에 한 줄로 붙인다 — `이 건은 12 slot. 가장 빠른 착수: 이해찬 8/31(월) → 9/15(화) 완료 예상`.
- 희망일정을 그보다 앞으로 넣으면 `그 날짜에 착수하려면 이은경이 7.5일 초과합니다`를 경고한다.
- 근거: 지금은 예약 확정이 구두 협의로 정해져, 확정 후에야 부하가 드러난다. 의뢰 시점에 보이면 되돌리는 비용이 없다.
- 구현 규모: `resources.js`에 `earliestStart(slots)` 하나 추가 + `/api/resources`에 담당자별 큐 끝 날짜(이미 `eta`로 있음)를 재사용. 프런트는 `autoFillRound()` 옆에 안내 한 줄. **새 테이블·새 입력 없음.**

**제안 2 — 주간 리소스 보고 + 구체적 재배정 후보**
주간보고(금 18:00)와 인증 통계 보고(월 09:00)에 리소스 섹션을 붙이고, **부하 편차가 임계를 넘을 때만** 재배정 후보를 함께 싣는다.

- `부하 편차 12 slot — 이은경 24일 / 이해찬 4일. RS-300(4 slot)을 이해찬에게 옮기면 편차 12 → 4`처럼 **옮길 건과 그 효과를 숫자로** 제시한다.
- 후보 선정은 단순 그리디로 충분하다 — 최다 부하자의 건 중 가장 늦은 일정 건을 최소 부하자에게 옮겨 보고 편차가 가장 줄어드는 조합 1~2개.
- 근거: 11차 화면은 "누가 과부하인지"까지만 보여준다. 재배정 판단은 아직 사람이 표를 보고 머리로 한다. 편차와 후보를 메일로 밀면 화면을 열지 않아도 결정이 된다.
- 구현 규모: `report.js`에 섹션 하나 + `resources.js`에 `rebalanceHints()` 하나. 발송 경로·수신자·서식은 기존 것을 그대로 탄다. **9차에서 정리한 규칙(자동발송은 `mailHtml`, 모델명·실명은 사내 복사본에만)을 반드시 따라야 한다** — 리소스 섹션에는 담당자 실명이 들어가므로 자동발송 본문에는 이름 대신 집계만 싣고 상세는 링크로 넘긴다.

두 제안 모두 **사용자 승인 전에는 착수하지 않는다.** 특히 제안 2는 메일 본문에 실명이 들어갈 수 있어 8차·9차에서 정한 사내 자료 보호 원칙과 맞물린다.

### 재수정 — 1주 가용 = 100% 기준 + 일별 가용 현황 (같은 날, 3차 요청)
사용자가 세 가지를 확정·추가했다.
1. **"전체 리소스 100"을 1주 가용(4명 × 5일 = 20 slot) 기준으로.** 앞서 내가 제시한 대안 쪽을 택한 것이다.
2. **일별로 가용 리소스 현황도 필요.**
3. **"담당별로 리소스가 비어있는 프로젝트에 자동으로 할당될 예정이라 담당자별로 리소스가 편중되지는 않을 예정."**

**3번이 화면의 방향을 바꿨다.** 직전 회차에서 만든 쏠림 지표(`초과 합계`·`쏠림 폭`, 균등분담선 대비 편차)는 **재배정 판단용**이었는데, 자동 할당이 그 일을 대신한다면 볼 이유가 없다. 그래서 그 지표들을 API·화면에서 **제거**하고, 대신 **여유(빈 slot)** 를 전면에 뒀다 — 자동 할당이 "어디에 넣을 수 있는지"가 이제 이 화면의 용도다. 쓰지 않는 필드를 남겨 두면 다음 사람이 그게 지표라고 착각한다.

**분모 전환.** `week_capacity = 인원수 × 5`(=20 slot)를 100%로 놓았다. 전체는 `사용률 = 미완 ÷ 20`, 담당자별은 `사용률 = 할당 ÷ 5`가 된다. 이은경 12 slot이면 240%로 "2.4주치 밀려 있다"가 바로 읽힌다. 앞 회차에서 이 방식을 택하지 않은 이유("1주라는 임의 상수")는 사용자가 그 상수를 직접 지정하면서 해소됐다.

**일별 배치 규칙 — 여기가 이번 회차의 실질적 설계다.** 일별 현황을 내려면 slot을 날짜에 펼쳐야 하는데, 그러려면 배치 모델이 필요하다.

- **한 담당자는 하루 1 slot만 쓴다(직렬 큐).** 담당자별로 의뢰를 대표 일정 순으로 줄 세워 영업일에 이어 붙인다. 시작은 `max(plan_date, 기준일)`. 이 모델이면 **일별 사용량이 가용을 넘지 않는다**(테스트로 고정) — 대신 과부하는 "뒤로 밀림"으로 드러나고, 그게 `예상 소진일`이다. 병렬 계상(겹치면 가용 초과로 표시)보다 이쪽이 현실과 맞고 "언제 누가 비는지"를 정확히 낸다.
- **미배정 건은 가장 빨리 비는 담당자 자리에 `배정 예정`으로 채운다.** 사용자가 자동 할당 예정이라 했으니 이게 그 미리보기다. 실제 배정이 아니라 예측이므로 색과 라벨을 달리 뒀다. 이걸 넣지 않으면 일별 사용률이 배정분만 반영해 실제보다 낮게 보인다.
- **각 날짜에 그날 비어 있는 담당자를 함께 표시**한다. 자동 할당이 어디에 넣을 수 있는지가 화면에서 바로 보이게 하려는 것이다.

**버그 — 기타 테스터 물량 2 slot이 조용히 사라졌다.** 일별 배치 1단계가 `MEMBERS`만 순회해서, 4명 외 테스터(김지윤 등)에게 걸린 건이 레인에도 `overflow`에도 안 들어갔다. `배치분 + 구간초과 = 전체 물량` 불변식을 테스트로 세워 뒀더니 `19 + 0 vs 21`로 바로 잡혔다. **판단: 배치에서 빼는 것 자체는 맞다** — 일별 가용은 '인증 담당 4명'의 가용이고 기타 테스터의 작업은 그 가용을 쓰지 않는다. 다만 조용히 빠지면 안 되므로 `daily.excluded`(건수·slot·이름)로 내보내고 화면에 명시했다. 불변식도 `배치분 + 구간초과 + 제외분 = 전체 물량`으로 갱신했다.

**조회 구간(horizon)은 5~60 영업일로 제한**한다(기본 20 = 4주). 구간을 넘어간 물량은 `overflow`로 잡아 화면에 표기한다 — 화면에 안 보이는 물량이 있다는 사실 자체가 정보다.

## 실행
- `npm install` 후 `npm start` (기본 PORT 3000, HOST 0.0.0.0). 환경변수 PORT/HOST로 변경 가능.
- 이메일 발송은 config.json의 smtp 설정 필요(미설정 시 앱·스케줄러는 정상 동작하되 발송만 생략).
